{
  "name": "server",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "dotenv -e .env.pod -- node dist/server.js",
    "build": "tsc && bun run sentry:sourcemaps",
    "sentry:sourcemaps": "sentry-cli sourcemaps inject --org free-sleep --project server ./dist && sentry-cli sourcemaps upload --org free-sleep --project server ./dist",
    "build:pr": "tsc",
    "dev": "MODE=dev dotenv -e .env.pod -- nodemon --watch src --ext ts --exec \"node --loader ts-node/esm\" src/server.ts",
    "dev:local": "dotenv -e .env.local -- nodemon --watch src --ext ts --exec \"node --loader ts-node/esm\" src/server.ts",
    "lint": "eslint src/ --ext .ts,.tsx",
    "migrate": "dotenv -e .env.pod -- prisma migrate dev --name",
    "migrate:deploy": "dotenv -e .env.pod -- prisma migrate deploy",
    "generate": "dotenv -e .env.pod -- prisma generate",
    "migrate:local": "dotenv -e .env.local -- prisma migrate dev --name",
    "generate:local": "dotenv -e .env.local -- prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "volta": {
    "node": "24.11.0"
  },
  "dependencies": {
    "@prisma/client": "^6.4.0",
    "@sentry/node": "^10.22.0",
    "binary-split": "^1.0.5",
    "cbor": "^9.0.0",
    "chokidar": "^4.0.0",
    "cors": "^2.8.5",
    "dotenv-cli": "^8.0.0",
    "express": "^5.1.0",
    "lodash": "^4.17.0",
    "lowdb": "^6.1.0",
    "moment-timezone": "^0.5.0",
    "node-schedule": "^2.1.0",
    "prisma": "^6.4.0",
    "winston": "^3.17.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.0",
    "@types/express": "^5.0.0",
    "@types/lodash": "^4.17.0",
    "@types/node": "^24.0.0",
    "@types/node-schedule": "^2.1.0",
    "@typescript-eslint/eslint-plugin": "^5.62.0",
    "@typescript-eslint/parser": "^5.62.0",
    "@sentry/cli": "^2.57.0",
    "eslint": "^8.57.0",
    "nodemon": "^3.1.0",
    "ts-essentials": "^10.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.7.0"
  }
}
